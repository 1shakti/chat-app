import { HeaderContainer, HeaderTitle } from "./styles";

export default function Header() {
    return (
        <HeaderContainer>
            <HeaderTitle>Smarter.Codes</HeaderTitle>
        </HeaderContainer>
    )
}